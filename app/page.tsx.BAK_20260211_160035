import Image from "next/image";
import Link from "next/link";
import HomeBookingSteps from "@/components/HomeBookingSteps";
import HomeOtherDestinations from "@/components/HomeOtherDestinations";
import SiteFooter from "@/components/SiteFooter";

export default function HomePage() {
  return (
    <main className="text-white">
      {/* ================= HERO ================= */}
      <section className="relative min-h-[78vh] flex items-end">
        <Image
          src="/hero/hero-home.jpg"
          alt="Red Rocks transportation"
          fill
          priority
          className="object-cover"
        />

        {/* cinematic overlay */}
        <div className="absolute inset-0 bg-gradient-to-t from-black/85 via-black/45 to-black/10" />
        <div className="absolute inset-0 bg-[radial-gradient(900px_600px_at_15%_10%,rgba(0,229,255,0.12),transparent_60%)]" />
        <div className="absolute inset-0 bg-[radial-gradient(900px_600px_at_85%_0%,rgba(255,90,31,0.10),transparent_55%)]" />

        <div className="relative z-10 w-full">
          <div className="max-w-7xl mx-auto px-6 pb-16 md:pb-20">
            <div className="max-w-3xl">
              <h1 className="text-5xl md:text-7xl font-black italic uppercase leading-none tracking-tight">
                Party at Red Rocks
              </h1>

              <p className="mt-6 text-[16px] md:text-[20px] leading-relaxed text-zinc-200">
                Fixed pricing. Pro drivers. Reliable post-show return.{" "}
                <span className="font-bold">Shuttle seats $59–$65/pp</span> or{" "}
                <span className="font-bold">Private Suburban $499</span>.
              </p>

              {/* NOTE: no button row here — nav already has Book Shuttle */}
              <div className="mt-6 text-xs uppercase tracking-[0.32em] text-white/60">
                Denver pickups • Top Circle drop-off • Driver waits after encore
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* ================= RED ROCKS: 1-2-3 ================= */}
      <HomeBookingSteps />

      {/* ================= OTHER DESTINATIONS (small) ================= */}
      <HomeOtherDestinations />

      {/* ================= RED ROCKS FAQ TEASER ================= */}
      <section className="max-w-7xl mx-auto px-6 pb-16">
        <div className="bg-surface border-soft shadow-soft rounded-3xl p-6 md:p-8 hover:shadow-2xl hover:-translate-y-1 transition-all duration-300">
          <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
            <div>
              <p className="text-[11px] font-black uppercase tracking-[0.32em] text-zinc-400">
                Red Rocks info
              </p>
              <h2 className="mt-2 text-2xl md:text-3xl font-black tracking-tight">
                Red Rocks FAQ + pickup rules
              </h2>
              <p className="mt-2 text-zinc-300">
                What time to arrive, where we meet, what happens after the show, cancellation policy, and the
                “don’t-get-stuck” rules.
              </p>
            </div>

            <Link href="/guide/red-rocks-faq" className="btn-primary">
              Read the FAQ
            </Link>
          </div>
        </div>
      </section>

      <SiteFooter />
    </main>
  );
}
