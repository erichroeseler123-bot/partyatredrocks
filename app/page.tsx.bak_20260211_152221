import Image from "next/image";
import Link from "next/link";
import FeaturedServices from "@/components/FeaturedServices";
import HomeBookingSteps from "@/components/HomeBookingSteps";

export default function HomePage() {
  return (
    <main className="text-white">
      {/* ================= HERO ================= */}
      <section className="relative min-h-[82vh] flex items-end">
        <Image
          src="/hero/hero-home.jpg"
          alt="Red Rocks transportation"
          fill
          priority
          className="object-cover"
        />

        {/* cinematic overlay */}
        <div className="absolute inset-0 bg-gradient-to-t from-black/80 via-black/45 to-black/10" />
        <div className="absolute inset-0 bg-[radial-gradient(900px_600px_at_15%_10%,rgba(0,229,255,0.14),transparent_60%)]" />
        <div className="absolute inset-0 bg-[radial-gradient(900px_600px_at_85%_0%,rgba(255,90,31,0.10),transparent_55%)]" />

        <div className="relative z-10 w-full">
          <div className="max-w-7xl mx-auto px-6 pb-20 md:pb-24">
            <div className="max-w-3xl">
              <h1 className="text-5xl md:text-7xl font-black italic uppercase leading-none tracking-tight">
                Party at Red Rocks
              </h1>

              <p className="mt-6 text-[16px] md:text-[20px] leading-relaxed text-zinc-200">
                Premium Red Rocks rides with fixed pricing:
                <span className="font-bold"> $59–$65/pp seats</span>,{" "}
                <span className="font-bold">Private SUV $499</span>, plus Sprinters and buses for bigger groups.
                Zero parking stress — reliable return after the encore.
              </p>

              <div className="mt-8 flex flex-col sm:flex-row gap-3">
                <Link href="/book-shuttle" className="btn-primary">
                  Book Red Rocks Shuttle
                </Link>

                <Link
                  href="/private-suburban"
                  className="btn-ghost border-red-500/30 bg-red-500/10 text-red-100 hover:bg-red-500/15"
                >
                  Book Suburban — $499
                </Link>

                <Link href="/week" className="btn-ghost">
                  This week’s shows
                </Link>
              </div>

              <p className="mt-5 text-[12px] uppercase tracking-[0.30em] text-zinc-400">
                Pro drivers • Clean vehicles • Show-night timing
              </p>
            </div>
          </div>
        </div>
      </section>

      <HomeBookingSteps />

      {/* ================= SERVICES ================= */}
      <FeaturedServices />

      {/* ================= INTEL BRIDGE ================= */}
    </main>
  );
}
