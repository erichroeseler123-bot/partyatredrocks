import Link from "next/link";

type ServiceCard = {
  title: string;
  price: string;
  detail: string;
  bullets: string[];
  href: string;
  pill?: string;
  ctaLabel: string; // button label (must include price)
};

function Card({ s }: { s: ServiceCard }) {
  return (
    <div className="group relative overflow-hidden rounded-2xl border border-white/12 bg-white/[0.045] p-5 shadow-soft transition-all duration-300 hover:-translate-y-1 hover:border-white/18 hover:shadow-2xl hover:-translate-y-1 transition-all duration-300">
      {/* soft highlight */}
      <div className="pointer-events-none absolute -top-24 -right-24 h-56 w-56 rounded-full bg-white/6 blur-2xl opacity-0 transition-opacity duration-300 group-hover:opacity-100" />

      <div className="relative z-10 flex items-start justify-between gap-3">
        <div>
          {s.pill ? (
            <div className="mb-2 inline-flex items-center rounded-full border border-white/12 bg-black/20 px-2.5 py-1 text-[10px] font-black uppercase tracking-[0.22em] text-white/75">
              {s.pill}
            </div>
          ) : null}

          <h3 className="text-lg font-black tracking-tight text-white">{s.title}</h3>
          <p className="mt-1 text-sm text-white/70">{s.detail}</p>
        </div>

        <div className="shrink-0 text-right">
          <div className="text-[11px] font-black uppercase tracking-[0.22em] text-white/55">
            Price
          </div>
          <div className="mt-1 text-base font-black text-white">{s.price}</div>
        </div>
      </div>

      <ul className="relative z-10 mt-4 space-y-2 text-sm text-white/75">
        {s.bullets.map((b) => (
          <li key={b} className="flex gap-2">
            <span className="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-white/35" />
            <span>{b}</span>
          </li>
        ))}
      </ul>

      <div className="relative z-10 mt-5">
        <Link
          href={s.href}
          className="inline-flex w-full items-center justify-center rounded-2xl border border-white/12 bg-black/20 px-4 py-2.5 text-[12px] font-black uppercase tracking-[0.22em] text-white/90 transition-all duration-300 hover:border-white/20 hover:bg-black/30 hover:shadow-2xl hover:-translate-y-1 transition-all duration-300"
        >
          {s.ctaLabel} →
        </Link>
      </div>
    </div>
  );
}

export default function FeaturedServices() {
  const redRocks: ServiceCard[] = [
    {
      pill: "Most Popular",
      title: "Shared Bus to Red Rocks",
      price: "$59–$65 / person",
      detail: "Ride with other fans. Simple pickup, pro driver, clean timing.",
      bullets: ["Per-person seats", "Timed pickup windows", "Best value for most groups"],
      href: "/book-shuttle",
      ctaLabel: "Shared Bus $59–$65",
    },
    {
      title: "Red Rocks SUV (Your Group Only)",
      price: "$499 flat",
      detail: "Private SUV for your group. Door-to-door timing, no strangers.",
      bullets: ["Private vehicle", "Flexible pickup", "Best for couples / VIP"],
      href: "/private-suburban",
      ctaLabel: "SUV $499",
    },
    {
      title: "Red Rocks Sprinter Van",
      price: "$699 flat",
      detail: "More space, same professionalism. Great for bigger groups.",
      bullets: ["Group-ready", "Private ride", "Ideal for 8–12"],
      href: "/booking",
      ctaLabel: "Sprinter $699",
    },
    {
      title: "Red Rocks 20-Passenger Bus",
      price: "$1,099 flat",
      detail: "One vehicle. One driver. One plan for large groups.",
      bullets: ["Large groups", "Private transport", "Best for 13–20+"],
      href: "/booking",
      ctaLabel: "20-Pass $1,099",
    },
  ];

  const otherVenues: ServiceCard[] = [
    {
      title: "Any Venue SUV",
      price: "$250 flat (up to 7)",
      detail: "Mission Ballroom, Ball Arena, Ogden, Gothic, Fillmore — anywhere.",
      bullets: ["Up to 7 passengers", "Anywhere in Denver Metro", "One price, no surge"],
      href: "/book-all-venues",
      ctaLabel: "SUV $250",
    },
    {
      title: "Any Venue Sprinter Van",
      price: "$450 flat",
      detail: "More seats + more comfort for non-Red Rocks nights.",
      bullets: ["Group transport", "Private ride", "Best for 8–12"],
      href: "/booking",
      ctaLabel: "Sprinter $450",
    },
    {
      title: "Any Venue 20-Passenger Bus",
      price: "$1,000 flat",
      detail: "Best option for large groups doing other venues.",
      bullets: ["Large groups", "Private transport", "Single pickup plan"],
      href: "/booking",
      ctaLabel: "20-Pass $1,000",
    },
  ];

  return (
    <section className="relative z-10 max-w-7xl mx-auto px-6 -mt-16 pb-10">
      <div className="rounded-3xl border border-white/12 bg-white/[0.04] p-6 md:p-8 shadow-soft glow-accent hover:shadow-2xl hover:-translate-y-1 transition-all duration-300">
        <div className="flex flex-col md:flex-row md:items-end md:justify-between gap-4">
          <div>
            <p className="text-[11px] font-black uppercase tracking-[0.30em] text-white/55">
              Transportation Options
            </p>
            <h2 className="mt-2 text-3xl md:text-4xl font-black tracking-tight text-white">
              Would you rather ride with other fans, or keep the whole vehicle to your group?
            </h2>
            <p className="mt-3 max-w-2xl text-sm md:text-base text-white/70">
              Clear pricing. Professional drivers. Real show-night logistics.
            </p>
          </div>

          <div className="flex gap-3">
            <Link href="/book-shuttle" className="btn-primary">
              Book Shared Bus
            </Link>
            <Link
              href="/booking"
              className="hidden md:inline-flex items-center justify-center rounded-full border border-white/14 bg-black/15 px-5 py-2 text-[12px] font-black uppercase tracking-[0.22em] text-white/85 transition-all hover:bg-black/25 hover:border-white/20"
            >
              Get a Quote →
            </Link>
          </div>
        </div>

        <div className="mt-8">
          <div className="flex items-center justify-between gap-4">
            <h3 className="text-sm font-black uppercase tracking-[0.28em] text-white/65">
              Red Rocks
            </h3>
            <div className="h-px flex-1 bg-white/10" />
          </div>

          <div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-4">
            {redRocks.map((s) => (
              <Card key={s.title} s={s} />
            ))}
          </div>
        </div>

        <div className="mt-10">
          <div className="flex items-center justify-between gap-4">
            <h3 className="text-sm font-black uppercase tracking-[0.28em] text-white/65">
              Other Venues
            </h3>
            <div className="h-px flex-1 bg-white/10" />
          </div>

          <div className="mt-5 grid gap-4 md:grid-cols-3">
            {otherVenues.map((s) => (
              <Card key={s.title} s={s} />
            ))}
          </div>
        </div>

        <div className="mt-10 rounded-2xl border border-white/12 bg-black/15 p-5 md:p-6 hover:shadow-2xl hover:-translate-y-1 transition-all duration-300">
          <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
            <div>
              <p className="text-[11px] font-black uppercase tracking-[0.30em] text-white/55">
                Special Route
              </p>
              <h3 className="mt-1 text-2xl font-black tracking-tight text-white">
                Mishawaka Shared Bus
              </h3>
              <p className="mt-2 text-white/70">
                Per-person ride to The Mishawaka. Clean timing, reliable return, no chaos.
              </p>
              <div className="mt-2 text-sm font-black text-white">$65 / person</div>
            </div>

            <div className="flex gap-3">
              <Link
                href="/shuttles/mishawaka"
                className="inline-flex items-center justify-center rounded-full border border-white/14 bg-black/15 px-5 py-2 text-[12px] font-black uppercase tracking-[0.22em] text-white/90 transition-all hover:bg-black/25 hover:border-white/20"
              >
                Mishawaka $65 →
              </Link>
              <Link
                href="/booking"
                className="inline-flex items-center justify-center rounded-full border border-white/14 bg-black/10 px-5 py-2 text-[12px] font-black uppercase tracking-[0.22em] text-white/80 transition-all hover:bg-black/20 hover:border-white/20"
              >
                Group booking →
              </Link>
            </div>
          </div>
        </div>

        <div className="mt-6 flex flex-col md:flex-row md:items-center md:justify-between gap-3 text-[11px] uppercase tracking-[0.28em] text-white/45">
          <span>Professional drivers • Clear pricing • Real logistics</span>
          <span className="text-white/35">Need help? Use “Get a Quote”.</span>
        </div>
      </div>
    </section>
  );
}
